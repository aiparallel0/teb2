DB_PATH=teb2.db
# Replace with a random 32+ character string before running.
# teb2 will refuse to start with this placeholder. Generate with e.g.:
#   head -c 48 /dev/urandom | base64
SECRET=change_me_in_production
PORT=8080
WORKERS=4
